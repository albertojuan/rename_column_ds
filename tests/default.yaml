- all-test-network-signals-stored:
    max_bytes_read: null
    max_time: null
    sql: |
      SELECT 1 as check
      FROM signal_minute_endpoint
      HAVING count() != 10
    pipe:
      name: signal_minute_endpoint
      params:
        network: 'TestNetwork'

- asset-not-empty:
    max_bytes_read: null
    max_time: null
    sql: |
        SELECT asset
        FROM signal_minute_endpoint
        WHERE asset = ''
    pipe:
      name: signal_minute_endpoint
      params:
        network: 'TestNetwork'





